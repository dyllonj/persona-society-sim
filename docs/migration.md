# Migration notes

Moved: see [howto-migrate-legacy-data.md](howto-migrate-legacy-data.md), which
covers both the legacy prompt-schema conversion and removing legacy
`trade`/`econ_txn` records from old dump directories.
