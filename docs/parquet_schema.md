# Parquet schema reference

Moved and expanded: see [reference-data-schema.md](reference-data-schema.md).

The old version of this file only documented 5 of the 11 tables actually
written by `storage/log_sink.py` and didn't mention the SQL side, the
DDL-only/dead tables, or the separate `MetricTracker` output files. The new
doc covers all of it.
